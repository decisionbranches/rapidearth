"""
Python script that usually registers previously created models.
In our django framework, no models are needed because the data management is not handled in Django, and therefore none are registered.
"""

from django.contrib import admin

# Register your models here.
