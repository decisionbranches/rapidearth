"""
Python script ...
"""

import pickle
import numpy as np
import os

# Load cutout information and load KDTree
# cutout_data = np.load("satsearch/data_models/features.npz")
# cutout_identifiers = cutout_data['identifier']
# cutout_identifiers = cutout_identifiers[:, 0:2]
# kdtree = KDTree(cutout_identifiers)
# pickle.dump(kdtree, open("kdtree.p", "wb"))
# kdtree = pickle.load(open("satsearch/data_models/kdtree.p", "rb"))
